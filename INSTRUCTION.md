this app include 
- Auth(Sign In / Sign Up)
- Foto capture for User
- Maps API with Lattitude, etc
- CRUD System 
- Store, Get, Remove database using CRUD implementation and databse from Firebase Platform
- Klasifikasi untuk penanganan kasus -> sudah ditangani atau belum ('Ditangani', 'Belum', 'Diproses') -> ENUM 
- Push notification untuk setiap update klasifikasi.